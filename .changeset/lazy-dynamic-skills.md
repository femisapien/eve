---
"eve": patch
---

Load static and dynamic skill instructions through one in-memory lookup, retaining dynamic packages across durable resumes without opening a sandbox. Materialize dynamic supporting files on sandbox access, skip unchanged writes, and replace refreshed packages so removed files no longer remain accessible.
