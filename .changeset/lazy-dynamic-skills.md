---
"eve": patch
---

List and load dynamic skill instructions without opening a sandbox, including after durable resume. Materialize supporting files on sandbox access, skip unchanged package writes, and replace refreshed packages so removed files no longer remain accessible.
