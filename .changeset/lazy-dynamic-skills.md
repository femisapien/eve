---
"eve": patch
---

Load static and dynamic skill instructions through one in-memory lookup, retaining dynamic packages across durable resumes without opening a sandbox. Stage dynamic supporting files lazily, reconcile changed and removed files when agents share a sandbox, and reject packages that exceed the documented durable-state limits.
